enum NotchSmoothness { defaultEdge }

enum GapLocation { center }

/// [CircleButtonState] enums are use to identify center button is opened or closed.
enum CircleButtonState { open, closed }

/// [CircleButtonState] enums are use to identify center button is opened or closed.
enum CircleButtonAnimationState { idle, running }
